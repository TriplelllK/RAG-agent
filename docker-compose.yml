version: '3.8'

services:
  rag-u300-ktl1:
    build: .
    container_name: rag-u300-ktl1
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - ./storage:/app/storage
    restart: unless-stopped
